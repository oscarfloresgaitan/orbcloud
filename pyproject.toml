[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "orbcloud"
version = "0.0.1"

dependencies = [
 "numpy",
 "matplotlib"
]

[tool.setuptools]
package-dir = {"" = "src"}
